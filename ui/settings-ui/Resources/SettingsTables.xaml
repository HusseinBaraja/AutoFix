<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="SettingsTheme.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <SolidColorBrush x:Key="TableSurfaceBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="TableBorderBrush" Color="#DCE2EE" />
    <SolidColorBrush x:Key="TableTextBrush" Color="#111827" />
    <SolidColorBrush x:Key="TableMutedTextBrush" Color="#5D6B82" />
    <SolidColorBrush x:Key="TableAccentSoftBrush" Color="#F0EDFF" />
    <SolidColorBrush x:Key="TableFocusBorderBrush" Color="#6F54E8" />

    <Style x:Key="TableShell" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource TableSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource TableBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource SettingsCornerRadius}" />
        <Setter Property="Padding" Value="1" />
        <Setter Property="ClipToBounds" Value="True" />
    </Style>

    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource TableSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource TableBorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="GridLinesVisibility" Value="None" />
        <Setter Property="RowHeaderWidth" Value="0" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="CanUserResizeRows" Value="False" />
        <Setter Property="ColumnHeaderHeight" Value="38" />
        <Setter Property="RowHeight" Value="38" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource TableTextBrush}" />
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="#F8FAFD" />
        <Setter Property="Foreground" Value="{StaticResource TableMutedTextBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="12,0" />
        <Setter Property="BorderBrush" Value="{StaticResource TableBorderBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Padding" Value="12,0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource TableSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="#EEF1F6" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#F7F8FC" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource TableAccentSoftBrush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="Background" Value="{StaticResource TableAccentSoftBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource TableFocusBorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
